# specialperiod 模块问题清单 — 后端代码生成工作流修改指引

> **收件**：后端生成器（`ai-developer/ai-workflow`）维护者
> **来源**：specialperiod 模块代码审查 + 生成器产物对比
> **目标**：定位生成器层面的根因，给出规范文档与自检逻辑的修改点

---

## 一、背景与结论

specialperiod 模块由 `ai-developer/ai-workflow` 生成，产物落在 `ai-generated-modules/blade-specialperiod`，部署到 `blade_lxqt` 时去掉了 `pojo/` 包前缀。审查发现该模块 CRUD 基本不可用。

**关键事实**：生成器产物与部署产物**逐字一致**（仅包路径差 `pojo/` 前缀），即所有问题都是生成器原生产出，**非手改引入**。

**核心结论**：生成器产出的 5 件套（Entity/VO/QVO/IVO/UVO）**内部字段命名与类型不一致**，Wrapper 用 `BeanUtil.copy` 按字段名复制时丢字段，整条数据流断裂。这是后端工作流的根因。前端工作流忠实消费了这个坏契约，不是前后端适配问题。

**对照证据**：同一生成器产出的 `blade-permit/WorkPermit` 模块，`WorkPermitVO extends WorkPermit`，字段天然一致、运行正常；而 `SpecialPeriodVO implements Serializable` 且把字段改名（`periodName`→`name`、`periodType`→`type`），翻车。说明规范文档**没有硬性约束 VO 字段名必须与 Entity 一致**，LLM 有自由发挥空间，偶尔出错。

---

## 二、字段分裂对照表

| 业务含义 | 数据库列 | Entity | VO | IVO / UVO | QVO |
|---|---|---|---|---|---|
| 时段名称 | `period_name` | `periodName` | `name` | `name` | `periodName` |
| 时段类型 | `period_type` | `periodType` | `type` | `type` | `periodType` |
| 是否启用 | `is_enabled` | `isEnabled` | `enabled` | — | — |
| 时段状态 | `period_status` | `periodStatus` | （无） | — | — |
| 生效星期 | （表无此列） | （无） | `weekDays` | — | — |
| 优先级 | （表无此列） | （无） | `priority` | — | — |

- Entity / DDL：`blade_lxqt/blade-service-api/blade-specialperiod-api/.../entity/SpecialPeriod.java:25-50`
- VO：`.../vo/SpecialPeriodVO.java:26-51`
- IVO：`.../vo/SpecialPeriodIVO.java:22-35`（`name`/`type` + `LocalDate`）
- Wrapper：`.../wrapper/SpecialPeriodWrapper.java:30-57`（全用 `BeanUtil.copy`）

后果链：新增时 `name→periodName` 拷贝失败 → `period_name NOT NULL` 报错；列表 `periodName→name` 拷贝失败 → 名称列全空；编辑回填后再提交 → 把 `periodName` 改成 null。

---

## 三、问题清单（按优先级）

每条给出：**现象 / 规范缺口 / 修复（规范文档 + 生成器自检）/ 影响**。

### B1【CRITICAL · 根因】VO/IVO/UVO 字段名与 Entity 不一致

- **现象**：见对照表。Entity 用 `periodName/periodType/isEnabled`，VO 用 `name/type/enabled`，IVO/UVO 用 `name/type`。
- **规范缺口**：`bladex-docs/bladex-data-layer.md` 的 VO 示例（`OrderVO`）字段名确实与 Entity 一致，但**未显式声明硬约束**，也未要求 `VO extends Entity`。LLM 生成 specialperiod 时改了名。
- **修复**：
  1. **规范文档**：在 `bladex-data-layer.md` 的 VO/IVO/UVO 章节顶部加 Hard Rule——"业务字段名必须与 Entity 逐字段同名；推荐 `VO extends Entity` 仅追加展示字段（参考 `permit/WorkPermitVO`）；若 `implements Serializable`，字段必须与 Entity 同名同类型"。
  2. **生成器自检**：生成后解析 Entity 字段集 `E`，与 VO/IVO/UVO 字段集 `V` 比对，业务字段（排除 Base 书写字段 id/createTime/updateTime/createUser/updateUser/status/isDeleted/tenantId）必须 `V ⊆ E` 且同名；不符则在审查阶段报错拦截，不允许落盘。
- **影响**：不修则新增报错、列表空、编辑清空——整个 CRUD 不可用。

### B2【CRITICAL】IVO/UVO 字段类型与 Entity 不一致

- **现象**：IVO/UVO 的 `startDate/endDate` 是 `LocalDate`，Entity 是 `Date`。`BeanUtil.copy` 跨类型拷不动。
- **规范缺口**：规范未约束 IVO/UVO 字段类型必须与 Entity 对应字段一致。
- **修复**：
  1. **规范文档**：在 IVO/UVO 章节加约束"字段类型必须与 Entity 对应字段一致；日期类型统一用 `Date`（与目标项目 blade_lxqt 既有约定一致）"。
  2. **生成器自检**：比对 IVO/UVO 与 Entity 同名字段的类型，不一致则拦截。

### B3【HIGH】VO 凭空多出数据库不存在的字段

- **现象**：VO 有 `weekDays`/`priority`/`createDept`，DDL 和 Entity 都没有对应列，永远取不到值。
- **规范缺口**：规范未约束 VO 字段必须有据可循。
- **修复**：
  1. **规范文档**：加约束"VO 每个字段必须来自：Entity 字段、或 Base 书写字段、或明确的展示衍生字段（如 `xxxName`/`xxxStatusName`）；禁止凭空新增业务字段。如需新增表列，必须同步 DDL"。
  2. **生成器自检**：VO 字段集必须 `⊆ Entity字段 ∪ 展示字段（后缀 Name 等）∪ Base字段`，否则拦截。

### B4【HIGH】Controller 端点未调用含校验的 Service 方法

- **现象**：`ISpecialPeriodService.java:18` 定义了 `submit()`（内含 `checkOverlap` 时段重叠校验），`SpecialPeriodServiceImpl.java:25-31` 也实现了；但 `SpecialPeriodController.java:72-87` 的 `/save`、`/update` 直接调基类 `save()`/`updateById()`，绕过 `submit()`，校验成死代码。
- **规范缺口**：`bladex-business-layer.md` 的 save/update 示例直接 `save(Wrapper.entity(IVO))`，未提及"Service 有业务校验方法时端点必须调用它"。
- **修复**：
  1. **规范文档**：加约束"Service 若定义了带业务校验的方法（如 `submit`/`xxxSave`），Controller 端点必须调用该方法，而非基类 `save/updateById`"。
  2. **生成器自检**：Service 接口中的自定义方法若未被任何 Controller 方法引用，则告警。

### B5【HIGH】Feign Client 定义了但 Controller 未实现

- **现象**：`ISpecialPeriodClient.java:9-31` 暴露 `GET /feign/client/specialperiod/check-special-period` 和 `/ping`，但 Controller `@RequestMapping("/specialperiod")` 下**无这两个端点**。其他微服务通过 Feign 调用会 404。且签名对不上：Feign `checkSpecialPeriod(String date)` 返回 `R<SpecialPeriod>`，Service `checkSpecialPeriod(LocalDateTime)` 返回 `boolean`。
- **规范缺口**：`bladex-feign.md` 未强制"每个 Feign Client 方法必须有 Controller 实现"。
- **修复**：
  1. **规范文档**：加约束"api 模块中每个 `IXxxClient` 方法，service 模块必须有匹配的 `@RequestMapping(API_PREFIX + "...")` 端点实现，且入参/返回类型一致"。
  2. **生成器自检**：扫描 api 模块所有 `IXxxClient` 方法，在 service 模块查找对应端点，缺失或签名不符则拦截。

### B6【HIGH】`period_status` 状态机无推进机制

- **现象**：`SpecialPeriodMapper.xml:53-61` 的 `selectActivePeriods` 依赖 `period_status = 2`（进行中），但数据库默认 `period_status = 1`（未开始），全工程**无定时任务/触发器**把状态从 1→2→3 推进。该查询永远返回空，"判断当前是否处于特殊时段"的核心功能失效。`service/timed` 包是空目录。
- **规范缺口**：规范未约束"状态机字段必须有推进机制"。
- **修复**：
  1. **规范文档**：加约束"含状态机字段（`xxxStatus` 有多个枚举值且依赖时间流转）的模块，必须生成配套定时任务（`timed` 包 `@Scheduled` 或走 blade-xxljob）推进状态；或在审查阶段强制提示'该状态字段如何推进'"。
  2. **生成器自检**：检测到状态机字段 + `selectXxxByStatus` 类查询，但 `timed` 包为空，则告警或强制生成定时任务骨架。

### B7【MEDIUM】`selectSpecialPeriodPage` + QVO 是死代码

- **现象**：`SpecialPeriodMapper.java:21` 定义了 `selectSpecialPeriodPage(IPage, QVO)`，XML 也写了，`SpecialPeriodQVO.java:29-39` 还有 `startDateStart/startDateEnd/endDateStart/endDateEnd` 区间字段；但 `Controller /list`（`SpecialPeriodController.java:56-67`）用 `Condition.getQueryWrapper(params, SpecialPeriod.class)` + `page()`，**没调用** `selectSpecialPeriodPage`。QVO 区间字段前端传了也被忽略，区间搜索无效。
- **规范缺口**：规范文档同时给了两种 list 范式（`Condition.getQueryWrapper` 和自定义 `selectXxxPage`），未要求二选一一致。
- **修复**：
  1. **规范文档**：加约束"`/list` 端点与 Mapper 自定义分页方法必须一致：要么用 `Condition.getQueryWrapper(params, Entity)` 且 Mapper 无自定义分页；要么用 `selectXxxPage(QVO)` 且 QVO 字段全部在 XML 生效"。
  2. **生成器自检**：Mapper 有自定义分页方法但 Controller `/list` 没调用，则告警。

### B8【MEDIUM】`selectActivePeriods` resultMap 返回类型不匹配

- **现象**：`SpecialPeriodMapper.xml:53-61` 的 `resultMap` type 是 `SpecialPeriod`（entity），但 `SpecialPeriodMapper.java:41` 方法返回 `List<SpecialPeriodVO>`，且 VO 字段名与列名不匹配（`name` vs `period_name`）。
- **修复**：随 B1 修字段名后，把 `resultMap` type 与方法返回类型统一（都改 entity，或都改 VO 且字段名对齐）。

---

## 四、specialperiod 模块本身的修复路径

修完生成器规范 + 自检后，二选一处理已生成的 specialperiod：

- **路径 A（推荐）**：重新生成。统一字段名为 `periodName/periodType/isEnabled/periodStatus`；VO 改 `extends SpecialPeriod` 或同名；补 Feign 端点实现、`period_status` 定时任务、`/list` 改用 `selectSpecialPeriodPage`。
- **路径 B（应急）**：手改现有代码，改动点对应 B1-B8。治标不治本，下个模块还会犯。

---

## 五、其他观察（非 specialperiod 必修，供参考）

- **版本适配一致性**：specialperiod 正确用了 Swagger 2（`@ApiModel`/`@ApiModelProperty`）+ `javax.validation`，适配了 blade_lxqt 的旧版（Java 8）；但同一生成器产出的 `blade-permit/WorkPermit` 用了 OpenAPI 3（`@Schema`）+ `jakarta.validation`，与 blade_lxqt 不符。建议生成器确保**按目标项目实际版本选注解**（`bladex-data-layer.md` 顶部已提到"参考项目版本约束优先级高于本文档"，但 permit 未执行）。

---

## 六、验证清单（修完生成器后回归）

- [ ] 生成新模块，VO/IVO/UVO 业务字段名 == Entity 字段名（或 `VO extends Entity`）
- [ ] IVO/UVO 字段类型 == Entity 对应字段类型
- [ ] VO 无"表里不存在的业务字段"
- [ ] Controller 端点调用了所有 Service 自定义校验方法
- [ ] 每个 Feign Client 方法都有 Controller 实现，签名一致
- [ ] 状态机字段有推进机制（定时任务或显式流转）
- [ ] `/list` 与 Mapper 自定义分页二选一一致，QVO 字段全部生效
- [ ] Mapper resultMap type 与方法返回类型一致
- [ ] 生成产物按目标项目版本选 Swagger 注解 + validation 包
