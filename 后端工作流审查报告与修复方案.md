# 后端代码生成工作流审查报告与修复方案

> **收件**:后端生成器(`ai-developer/ai-workflow`)维护者
> **来源**:3 个独立 subagent(架构 / 校验机制 / 工程化)各自读代码下结论 + 交叉验证 + 对 B1–B8 改动的批判性复盘
> **日期**:2026-07-10
> **目标**:在《后端工作流修改清单.md》(B1–B8,已落地自检规则)之上,定位工作流**系统性/根本性**缺陷,给出分级修复方案与实施路线图

---

## 一、审查方法与范围

- **方法**:3 个独立 subagent 分别从「架构与数据流」「校验与修复机制」「工程化与质量保障」三视角审查,互不知晓彼此结论;随后交叉验证,合并去重,并对刚完成的 B1–B8 改动做客观复盘。
- **范围**:`ai-developer/ai-workflow/` 全部主代码(~5300 行) + 测试(36 个纯单元测试) + 配置 + 近期 git history。
- **基线**:工作流自首发后本体无迭代(近期 8 个提交 7 个是部署脚本),B1–B8 自检为最新增量。

---

## 二、总体结论

**局部精致、整体脆弱**。单点(FileWriteExecutor 路径防护、回调 SSRF 防护与重试、拓扑排序的 Part A ID 映射、CrossFileValidator 契约规则密度)设计扎实;但作为"AI 写代码"系统,**最核心的质量门(编译验证)被完全跳过**,生成代码"能否编译运行"没有任何自动化保障,全靠下游人工 `mvn compile` 兜底。叠加 LLM 非确定性零防护、崩溃恢复缺失、并发竞态、God Object,系统的可靠性保证严重不足。

B1–B8 自检规则是"在脆弱基础上叠加更多启发式",**提升了检出能力**(测试证明能检出 specialperiod 真实 bug),但**没触及根本**(编译验证缺失),且新加的修复循环继承了现有设计缺陷(详见第五节复盘)。

---

## 三、问题与修复方案(按严重级别)

> 每条:**现象 / 证据 / 影响 / 修复方案(方向+改动文件+注意) / 优先级 / 工作量**

### CRITICAL

#### C1 编译验证完全禁用,生成代码带着编译错误落盘
- **现象**:`BuildVerifier`(132 行,已实现 `mvn compile -pl ... -am` + 错误解析)的调用被注释,整类沦为死代码。所有"可编译"承诺只靠正则(Layer1)+ JavaParser 无 symbol solver 的启发式(Layer2)。
- **证据**:`BladeXCodeAgent.java:393-396`(`// BuildResult buildResult = buildVerifier.verify(...)` + "编译验证跳过");`CrossFileValidator.java:34-36` 自述"不依赖完整 classpath"。
- **影响**:类型不兼容、方法体引用不存在符号、缺依赖、SQL 语法错等决定可编译性的错误**系统性检不出**,生成代码直接落盘,下游 `mvn compile` 才暴露。对 REAL 模式是直接污染真实项目。
- **修复方案**:
  - **方向**:启用 BuildVerifier 作为最终门禁。ISOLATED 模式 plan 完成后对 `outputRoot` 受影响模块跑增量编译;REAL 模式对目标项目受影响模块跑。编译失败标记 `COMPLETED_WITH_ERRORS` 并触发修复(或回滚该 plan 写盘)。
  - **改动文件**:`BladeXCodeAgent.executeWorkflow`(末尾调 `buildVerifier.verify`)、`BuildVerifier`(模块发现逻辑适配 ISOLATED 多模块结构)、`enums/PlanStatus`(加 `COMPLETED_WITH_ERRORS`)、`FileWriteExecutor`(提升 snapshot 为 plan 级以支持回滚)。
  - **注意**:ISOLATED 产物是孤立模块,需构造临时聚合 pom 或用 `-pl` 显式指定;编译耗时增加 plan 总时长,可异步/后台;REAL 模式编译失败回滚需 plan 级快照(当前仅单文件级)。
- **优先级**:P0 | **工作量**:大

#### C2 无崩溃恢复 + 无事务边界,EXECUTING 状态 plan 永久卡死
- **现象**:`executeWorkflow` 数十次 DB 写各自 auto-commit,无事务包裹;`inFlight` 是内存态 `ConcurrentHashMap.newKeySet()`,重启即失;无启动扫描清理 stale EXECUTING。
- **证据**:`PlanExecutionServiceImpl.java:62`(inFlight 内存态)、`:167-186`(@Async 无事务)、`BladeXCodeAgent.java:96-188`(executeWorkflow 无 @Transactional);无 ApplicationRunner/CommandLineRunner 做恢复。
- **影响**:进程崩溃/OOM/kill -9 后 plan 卡在 EXECUTING 永不恢复,UI 永远显示"执行中",`markPlanFailed` 对 kill -9/OOM/断电无效。磁盘已写文件与 DB 状态不一致。
- **修复方案**:
  - **方向**:(1) 新增 `StartupRecoveryRunner implements ApplicationRunner`,启动时扫描 `status in (EXECUTING)` 的 plan/subPlan,标记 FAILED 并记"进程重启恢复";(2) 关键状态流转(plan EXECUTING->终态、subPlan 终态)用 `@Transactional` 保证原子性;(3) `inFlight` 改 DB 级锁(plan 行级 `SELECT ... FOR UPDATE` 或状态字段 CAS)。
  - **改动文件**:新增 `config/StartupRecoveryRunner`、`PlanExecutionServiceImpl`(事务边界)、`AiPlan`/`AiSubPlan`(状态 CAS)。
- **优先级**:P0 | **工作量**:中

#### C3 REAL 模式查重-写盘 TOCTOU 竞态,并发可覆盖真实项目代码
- **现象**:REAL 模式 `detectNameConflicts`(force 重扫)与 `fileWriteExecutor.write` 之间存在时间窗口,线程池 max4 允许 4 个 plan 并发,两个 REAL plan 针对同一目标项目可同时通过查重后写入同名文件,后者静默覆盖前者。
- **证据**:`BladeXCodeAgent.java:344-358`(查重)+ `:370-373`(写盘);`AiWorkflowConfiguration.java:84-95`(maxPoolSize=4)。
- **影响**:真实项目代码被静默覆盖,数据丢失。REAL 模式"绝不覆盖现有代码"的核心承诺在并发场景下失效。
- **修复方案**:
  - **方向**:对同一 `targetProjectRoot` 加进程级写锁(或 plan 级串行队列),使"查重+写盘"在整个 plan 范围内互斥;或用文件系统级 `Files.createFile`(CREATE_NEW)原子操作替代 check-then-write。
  - **改动文件**:新增 `agent/ProjectWriteLockManager`、`BladeXCodeAgent`(detectNameConflicts+write 包在锁内)。
  - **注意**:锁粒度按 targetProjectRoot,不阻塞不同项目的并发;ISOLATED 模式不受影响(隔离区本就允许覆盖)。
- **优先级**:P0 | **工作量**:中

---

### HIGH

#### H1 LLM 非确定性零防护 + 核心编排零集成测试
- **现象**:36 个测试全是纯单元(无 Spring/LLM/DB/端到端);`executeWorkflow`/拓扑/三个修复循环零集成测试;`glm-latest` 是别名,上游换版即质量突变,无 golden test/回归测试。
- **证据**:`src/test/` 仅 3 个测试文件;`application.yml:45,51` model=glm-latest、temperature=0.1。
- **影响**:同一 plan 两次生成可能完全不同;LLM 升级回归无测试发现;无法证明端到端主流程能跑通。
- **修复方案**:
  - **方向**:(1) golden test:固定 plan + mock LLM 响应,断言生成文件可编译且契约校验通过;(2) LLM 升级跑回归;(3) `executeWorkflow` 主路径 mock LlmClient 集成测试,覆盖拓扑/失败传播/修复循环收敛。
  - **改动文件**:新增 `WorkflowIntegrationTest`(mock `LlmClient` 返回预制响应)。
  - **注意**:依赖 C1(编译验证)才能断言"可编译";mock LLM 响应要覆盖"修好 A 改坏 B"场景验证修复回退(M1)。
- **优先级**:P1 | **工作量**:中

#### H2 修复循环不重跑 ConventionValidator,可引入单文件违规落盘
- **现象**:三个修复循环(`retryCrossFileContractMismatches`/`retryPlanWideEntityDdlMismatches`/`retryPlanWideVoEntityMismatches`)修复成功后只跑 `crossFileValidator.validate`,从不重跑 Layer1。LLM 重生成时可能丢掉 `extends BladeController`/`@AllArgsConstructor`/`@TableName`/`@EqualsAndHashCode(callSuper=true)`/UVO 的 `id`。
- **证据**:`conventionValidator.validate` 仅在 `BladeXCodeAgent.java:241` 调用一次;三个 retry* 方法 replaceFile 前无 Layer1 校验。
- **影响**:修一个跨文件问题、引入一个单文件硬错误,违规落盘且无信号。**含本次新加的 VO 修复循环**。
- **修复方案**:
  - **方向**:每次 `fix.getGeneratedFiles().get(0)` 成功后,对新内容跑 `conventionValidator.validate(file)`;不通过则视为修复失败(回退到上一版本或继续重试),不得直接 `replaceFile`。
  - **改动文件**:`BladeXCodeAgent` 三个 retry* 方法(在 replaceFile 前插入 Layer1 校验,建议抽公共方法 `acceptFixOrRollback`)。
- **优先级**:P1 | **工作量**:小

#### H3 多类 ERROR 只记录不阻断,子方案仍标 COMPLETED
- **现象**:CrossFileValidator 的 ERROR 默认"仅记录不阻断写盘";子方案级校验后无论 errorCount 多少都继续写盘并标 COMPLETED。未覆盖修复的 ERROR:`CROSS-FEIGN-FALLBACK-MISSING`、`CROSS-WRAPPER-ENTITY-IVO/UVO`、`CROSS-MAPPER-XML-NAMESPACE`、`CROSS-IMPORT-CLOSURE-MISSING`、`MAPPER-RESULTMAP-TYPE-MISMATCH`(本次新增 B8)。
- **证据**:`BladeXCodeAgent.java:303-325`(仅记录)+ `:399`(仍标 COMPLETED)。
- **影响**:用户/Part A 看到 COMPLETED,实际代码含编译错误;只能翻 `execution_log` 才能发现,透明度严重不足。**本次 B8(ERROR)实际不阻断写盘**。
- **修复方案**:
  - **方向**:(1) ERROR 级跨文件问题反映到 plan/subplan 终态(如 `COMPLETED_WITH_ERRORS`);(2) Part A 接口(`/status`、`/files`)显著透出 ERROR 计数与清单,而非埋在日志文本;(3) 评估哪些 ERROR 应硬阻断(如 `CROSS-IMPORT-CLOSURE-MISSING` 必编译失败,应阻断)。
  - **改动文件**:`BladeXCodeAgent`(终态判定)、`enums/SubPlanStatus`/`PlanStatus`(加 `COMPLETED_WITH_ERRORS`)、`PlanReceiverController`/VO(返回 ERROR 清单)。
- **优先级**:P1 | **工作量**:中

#### H4 LLM 调用零重试,单次网络抖动致子方案失败
- **现象**:`LlmClient` 对 IOException 直接抛 RuntimeException,唯一重试是 `max_tokens` 400 减半;5xx/超时/连接重置均不重试,无指数退避。对比 Part A 回调有 3 次指数退避。
- **证据**:`LlmClient.java:109-112`。
- **影响**:LLM 是核心依赖,readTimeout 240s 内任意瞬时故障直接让原子任务失败 -> 子方案 FAILED;修复循环最多 4 次 LLM 调用放大失败概率。
- **修复方案**:
  - **方向**:对 5xx/IOException 加 2-3 次指数退避重试(与回调策略对齐),区分可重试(网络/5xx/超时)与不可重试(4xx 参数错);重试耗尽才抛。
  - **改动文件**:`LlmClient.generate`。
- **优先级**:P1 | **工作量**:小

#### H5 线程池满载静默丢弃,plan 永久卡 RECEIVED
- **现象**:AbortPolicy 满载抛 RejectedExecutionException,被 `PlanReceiverController.receive` 的 try-catch 吞掉,仍返回 ok;plan 已落库 RECEIVED 但未执行,Part A 轮询永远 RECEIVED。
- **证据**:`AiWorkflowConfiguration.java:90`(AbortPolicy)、`PlanReceiverController.java:61-69`(吞异常返回 ok)。
- **影响**:无告警、无自动恢复,需人工 `/api/execution/trigger`。
- **修复方案**:
  - **方向**:拒绝时返回 503(或 202+PENDING 状态)让 Part A 知道未执行;或落 PENDING 状态供启动恢复扫描(C2)重入队。
  - **改动文件**:`PlanReceiverController.receive` 的 try-catch(区分 RejectedExecutionException)。
- **优先级**:P1 | **工作量**:小

#### H6 trigger 端点无鉴权,可远程重触发 REAL 写真实项目
- **现象**:`POST /api/execution/trigger` 无 `AdminTokenGuard`,不重新校验 writeTarget;`receive` 对 REAL 模式鉴权但 trigger 不。
- **证据**:`PlanExecutionController.java:22-28`。
- **影响**:知道 receptionId 的攻击者可远程触发已落库的 REAL 模式 plan 重执行,写盘到真实项目根(默认 `blade_hgsjy`)。
- **修复方案**:
  - **方向**:trigger 端点加 `AdminTokenGuard`,或对 REAL 模式 plan 的 trigger 重新鉴权 + 二次确认。
  - **改动文件**:`PlanExecutionController.trigger`。
- **优先级**:P0 | **工作量**:小

#### H7 单例实例字段 cachedAdaptationSummary 并发竞态
- **现象**:`BladeXCodeAgent` 是单例 Bean,但有实例字段 `cachedAdaptationSummary`/`adaptationSummaryForPath`;volatile 只保证可见性,check-then-set 非原子;并发 plan 互相覆盖参考项目摘要。
- **证据**:`BladeXCodeAgent.java:525-547`;`AiWorkflowConfiguration.java:158`(@Bean 单例)。
- **影响**:某 plan 可能注入错误参考项目的适配摘要,生成代码风格/版本约束混乱,静默无报错。
- **修复方案**:
  - **方向**:缓存改 per-plan 局部变量(像 `ensuredSkeletonKeys` 那样在 `executeWorkflow` 内创建并传递,或封装到 `WorkflowContext` 对象),不要放单例实例字段。
  - **改动文件**:`BladeXCodeAgent`(getAdaptationSummary 改签名接受 context,移除实例字段)。
- **优先级**:P1 | **工作量**:小

#### H8 BladeXCodeAgent 1628 行 God Object,14+ 职责
- **现象**:单类同时承担编排/子方案执行/任务解析/校验修复循环(3 个)/落库/写盘/查重/参考摘要缓存/回调/实体名提取/拓扑排序。
- **证据**:`BladeXCodeAgent.java` 全文 1628 行。
- **影响**:违反 SRP,任何修改高概率引入回归;测试几乎不可能覆盖所有路径组合;新人理解成本巨大。
- **修复方案**:
  - **方向**:分阶段拆出 `SubPlanParser`(任务解析+实体名提取)、`RepairLoopExecutor`(通用化三个修复循环,见 M9)、`PlanRepository`(状态流转+落库)、`WorkflowNotifier`(回调)、`ConflictDetector`(查重);编排器只保留 DAG 执行+委托。
  - **改动文件**:`BladeXCodeAgent` 拆分 + 新增 5 个类。
  - **注意**:大重构,需 H1 集成测试兜底后再做,避免行为漂移。
- **优先级**:P2 | **工作量**:大

#### H9 parseAtomicTasks 靠标题中文关键字猜文件类型,脆弱
- **现象**:生成什么文件完全靠子方案标题字符串匹配(`contains("Entity")`/`("数据库")`/`("sql")`...);标题"用户管理"不含任何关键字走兜底生成单个 `Code.java`。
- **证据**:`BladeXCodeAgent.java:660-756`。
- **影响**:Part A 标题措辞稍变即漏生成文件,后续校验和编译都无法补救;本质是把自由文本标题当结构化协议用。
- **修复方案**:
  - **方向**:Part A 在 `SubPlanVO` 增加 `expectedFiles`/`taskTypes` 结构化字段显式声明预期产出,Part B 不再猜标题;过渡期保留关键字兜底。
  - **改动文件**:`PlanReceiveRequest.SubPlanVO`(加字段)、`BladeXCodeAgent.parseAtomicTasks`、前端 Part A(声明产出)。
- **优先级**:P2 | **工作量**:中(跨前后端)

---

### MEDIUM

#### M1 修复循环无条件保留输出,可能越修越坏无回退
- **现象**:三个 retry* 均 `replaceFile` 无条件替换,无"修复前后 issue 数对比/回退";maxReviewRetries=3 用尽后保留最差版本,原始版本已被覆盖。
- **修复方案**:每轮记录修复前该文件 issue 数;修复后若不降反升回退上一版本;重试用尽恢复原始版本。**改动**:三个 retry*(或抽象后统一)。**优先级**:P1 | **工作量**:小

#### M2 Controller->Service 契约只比"方法名+参数个数",漏检参数类型
- **现象**:`checkControllerServiceCalls` 用粗签名,参数类型不兼容(Integer vs Long)漏检;`methodFullSigs` 已收集却未用于规则7(规则8已用,标准不一致)。
- **修复方案**:规则7复用 `methodFullSigs` 对 Controller 调用做参数类型比对,与规则8同等严格。**改动**:`CrossFileValidator.checkControllerServiceCalls`。**优先级**:P2 | **工作量**:小

#### M3 Entity 修复破坏下游字段引用,无校验可见
- **现象**:Entity↔DDL 修复重生成 Entity 后,引用旧字段名的 ServiceImpl/Controller/Wrapper 全部编译失败,15 条规则无一条检方法体字段引用。
- **修复方案**:Entity 字段集合变更后,对同模块 ServiceImpl/Controller/Wrapper 重跑"方法体 setter/getter 是否在 Entity 存在"检查;或把 Entity 修复纳入下游重生成扩散。**改动**:`CrossFileValidator` 新规则 + `BladeXCodeAgent` 修复扩散。**优先级**:P2 | **工作量**:中

#### M4 BASE_SERVICE_METHODS 白名单三份且不一致(漏 deleteLogic)
- **现象**:白名单硬编码三处(`CrossFileValidator.java:504/581/972`),第二份漏 `deleteLogic`/`deleteLogicBatch`;ServiceImpl 调 `deleteLogic` 会被误报"接口未声明"。
- **修复方案**:白名单提为单一 `static final` 常量,补齐 `deleteLogic`/`deleteLogicBatch`。**改动**:`CrossFileValidator`。**优先级**:P1 | **工作量**:小

#### M5 Part A 回调 URL 无 SSRF 防护
- **现象**:LLM base URL 有完整 SSRF 防护(禁内网 IP),回调 URL 只检 scheme+host 非空;配置错误可向内网发 POST 泄露 plan 内容。
- **修复方案**:callback URL 复用 `isAllowedBaseUrl` 同款校验,或至少启动时 warn 非常规地址。**改动**:`PartACallbackServiceImpl`。**优先级**:P1 | **工作量**:小

#### M6 方案接收无幂等,Part A 重试产生重复 plan
- **现象**:`receivePlan` 每次生成新 UUID,无 dedup key;Part A 超时重试会创建第二个 plan 并执行两次。
- **修复方案**:用 `projectId + masterPlan.version` 或 content hash 做幂等 key,已存在返回原 receptionId。**改动**:`PlanExecutionServiceImpl.receivePlan`、`PlanReceiveRequest`。**优先级**:P2 | **工作量**:小

#### M7 plan 级修复 DB↔磁盘写盘非原子
- **现象**:`persistSingleFile`(写盘)与 `updateGeneratedFileContent`(更新 DB)两步非原子,各自 catch 异常仅 log;磁盘/DB 可一方成功一方失败,且日志有误导("DB 已更新"实际未更新)。
- **修复方案**:两步一个 try 块,任一失败回滚另一步(磁盘失败则不更新 DB 保留原内容;DB 失败则回滚磁盘);或引入"同步状态"字段。**改动**:`BladeXCodeAgent.persistSingleFile` + `updateGeneratedFileContent`。**优先级**:P1 | **工作量**:小

#### M8 extractCode 裸响应兜底会把散文写入文件 + JavaParser 解析失败静默跳过
- **现象**:LLM 返回无代码围栏的"散文+代码"时,`extractCode` 检测到 `package` 行即返回整段(含散文);JavaParser 解析失败时 `CrossFileValidator` `continue` 跳过,该文件所有跨文件规则绕过。损坏文件以"校验通过"假象落盘。
- **修复方案**:(1) 裸响应从首个 `package`/`import`/`public class` 截取到文件尾,丢弃前导散文;(2) 写入前 `JavaParser.parse()` 验证可解析性,失败视为 LLM 失败重试,而非写入后静默跳过。**改动**:`BladeCodeGenRouter.extractCode`、`BladeXCodeAgent`(写入前校验)。**优先级**:P1 | **工作量**:小

#### M9 规则散落三处 + 修复循环三处近似重复
- **现象**:新增一条可修复规则需改 CrossFileValidator(实现)+ BladeXCodeAgent(FIXABLE_RULES+修复循环)+ BladeCodeGenRouter(fix 方法)+ PromptBuilder(prompt),无注册机制;三个 retry* 是近似复制的 80 行循环。
- **修复方案**:引入 `ValidationRule` 接口 + 注册表;抽象 `RepairLoop` 模板方法(规则集 + fixer + postFixAction)。**改动**:大重构(与 H8 关联)。**优先级**:P2 | **工作量**:大

#### M10 平台 Cache 幻觉校验只看 import + 白名单两份
- **现象**:两层都只检 `import ...Cache;`,全限定用法 `org.springblade.system.cache.DeptCache.getList()` 漏检;白名单两份(CONVENTION 8 个 + CROSS 8 个)易漂移。
- **修复方案**:(1) 额外扫代码文本 `org\.springblade\.system\.cache\.\w+Cache` 全限定引用;(2) 白名单提单一常量两层共用;(3) CrossFileValidator 移除该重复规则(已由 Layer1 阻断)。**改动**:`ConventionValidator`、`CrossFileValidator`。**优先级**:P2 | **工作量**:小

#### M11 日志截断(10 条/4KB),大量 ERROR 时透明度不足
- **现象**:plan 级 issues `limit(10)` + `substring(0,4000)`,子方案级 `limit(5)`;几十个文件上百 ERROR 时关键错误被截断。
- **修复方案**:完整 issues 以 JSON 全量持久化到独立表/字段,UI 分页展示。**改动**:`execution_log` 表/字段、Part A 接口。**优先级**:P2 | **工作量**:中

---

### LOW

| 编号 | 问题 | 修复方向 | 优先级 |
|---|---|---|---|
| L1 | 死代码(~120 行未调用方法 `extractTargetPath`/`derivePathFromPkg`/`defaultPathForType`/`buildReverseDependencies` + BuildVerifier 整类) | 删除确认无调用方法;BuildVerifier 随 C1 启用 | P2 |
| L2 | `target-project-root` 默认值两处不一致(yml=`../../blade_hgsjy`,properties=`../../ai-generated-modules`) | 统一为隔离区;REAL 模式启动校验 root 非参考项目路径 | P1 |
| L3 | FileWriteExecutor 路径防护零测试(安全命脉无回归) | 补路径遍历测试(绝对路径/`..`/符号链接/白名单外扩展名/TOCTOU) | P1 |
| L4 | `catch(Throwable)` 吞 InterruptedException,影响优雅停机 | 捕获 InterruptedException 时 `Thread.currentThread().interrupt()` 恢复标志 | P2 |
| L5 | `voInstructions` QVO 分支硬编码 "OrderQVO",误导 LLM | 改用 `entityName` 参数 | P2 |
| L6 | `lineCount` 以 `\n` 计数偏差(末尾多算 1 行) | 用 `content.lines().count()` | P2 |
| L7 | FileWriteExecutor 白名单扩展名偏宽(含 html/css/vue/ts 等) | 收窄到 java/sql/xml/yml/properties | P2 |
| L8 | AdminTokenGuard 未配 token 时 localhost 绕过(容器风险) | REAL 写入要求 token 必须已配置,localhost 也不放行 | P1 |
| L9 | OkHttp 未配置 ConnectionPool/Dispatcher,高并发争用 | 显式配置 maxRequestsPerHost | P2 |
| L10 | CrossFileValidator 每次 `new JavaParser()` | 提为 static final 复用 | P2 |
| L11 | `autoCommit` 配置存在但 Git 提交未实现(类注释声称已实现) | 实现或移除配置项 | P2 |
| L12 | `CodeApplyController` 501 占位 | 实现或移除 | P2 |

---

## 四、优先级与实施路线图

### P0 — 止血(安全/数据丢失/核心质量门,建议立即)
- **C1 启用编译验证门**(大,但这是"AI 写代码"系统的核心承诺,不补则前面所有自检都是尽力而为)
- **C2 崩溃恢复 + 事务边界**(中)
- **C3 REAL 模式写锁消除 TOCTOU**(中)
- **H6 trigger 端点鉴权**(小)

### P1 — 可靠性与质量(建议紧随 P0)
- **H2 修复循环重跑 Layer1**(小,直接影响本次 VO 修复可靠性)
- **H3 ERROR 反映终态 + Part A 透出**(中)
- **H4 LLM 调用重试**(小)
- **H5 线程池拒绝可观测**(小)
- **H7 单例缓存竞态**(小)
- **H1 集成测试 + golden test**(中,为 H8 重构兜底)
- **M1 修复循环回退机制**(小)
- **M4 白名单统一补 deleteLogic**(小)
- **M5 回调 SSRF 防护**(小)
- **M7 DB↔磁盘原子性**(小)
- **M8 extractCode 散文截取 + 写入前解析校验**(小)
- **L2 target-project-root 默认值统一**(小)
- **L3 FileWriteExecutor 路径防护测试**(小)
- **L8 REAL 模式 token 强制**(小)

### P2 — 可维护性与功能增强(持续优化)
- **H8 拆分 God Object**(大,需 H1 兜底)
- **H9 parseAtomicTasks 结构化**(中,跨前后端)
- **M2 Controller-Service 参数类型比对**(小)
- **M3 Entity 修复下游扩散校验**(中)
- **M6 接收幂等**(小)
- **M9 规则注册 + 修复循环抽象**(大,与 H8 关联)
- **M10 Cache 幻觉全限定 + 白名单统一**(小)
- **M11 日志全量持久化**(中)
- **L1/L4-L7/L9-L12** 各项小优化

### 建议节奏
1. **第一阶段(P0)**:C1 + C2 + C3 + H6,目标是"不再丢数据、不再卡死、真实项目不被覆盖、编译错误能被发现"。C1 工作量最大,可先做 ISOLATED 模式编译,REAL 模式编译跟进。
2. **第二阶段(P1 可靠性批次)**:H2/H3/H4/H5/H7 + M1/M4/M5/M7/M8 + L2/L3/L8,这批多为小改动,集中消除可靠性盲点;H1 集成测试穿插其中为后续兜底。
3. **第三阶段(P2)**:H8 拆分 + H9 结构化 + M9 抽象,在 H1 集成测试覆盖后做大重构。

---

## 五、对本次 B1–B8 改动的客观复盘

独立审查揭示了我作为修改者的盲点,诚实纠正如下:

1. **"B1/B2/B3 阻断"表述不准确**。CrossFileValidator 的 ERROR 默认"仅记录不阻断写盘"。B1/B2/B3 是"检出 -> 进修复循环尝试修复 -> **修复失败则保留原文件落盘**",并非真正阻断。B8 标 ERROR 但同样不阻断。→ 由 **H3** 修复(ERROR 反映终态/可选阻断)。

2. **新加的 `retryPlanWideVoEntityMismatches` 照搬现有循环,继承三个缺陷**:不重跑 Layer1(H2)、无条件保留越修越坏(M1)、修复失败不阻断保留原文件。→ 由 **H2 + M1** 修复。

3. **Entity 修复的下游扩散未覆盖**:Entity 被DDL修复改字段后,VO 对齐了新 Entity,但 ServiceImpl/Controller 方法体仍是旧字段 -> 编译失败且无校验检出。→ 由 **M3** 修复。

4. **治本(规范文档+prompt)是软约束**:specialperiod 本就是"prompt 已写'VO 字段必须一致'但 LLM 仍违反"的反例。硬措辞仍是软约束,真正兜底靠自检--而自检不阻断。→ 由 **C1 编译验证** 作为最终兜底。

**结论**:B1–B8 改动是"在启发式校验层做了有价值的加法,测试证明能检出真实 bug",但没触及工作流最根本缺陷(编译验证缺失、一致性保证缺失),且新修复循环继承了现有设计缺陷。属有价值的增量,远非"根治"。

---

## 六、验证清单(实施后回归)

- [ ] C1:生成代码 plan 完成后跑 `mvn compile`,编译失败标记 `COMPLETED_WITH_ERRORS` 且 Part A 可见
- [ ] C2:进程强杀后重启,原 EXECUTING plan 被标 FAILED 且日志记"进程重启恢复"
- [ ] C3:并发 2 个 REAL plan 写同一目标项目,第二个被写锁阻塞而非覆盖
- [ ] H1:mock LLM 的端到端集成测试通过;LLM 升级跑 golden test 回归
- [ ] H2:修复循环重生成文件后 ConventionValidator 重跑,丢注解的修复被拒绝
- [ ] H3:含 ERROR 的 plan 终态为 `COMPLETED_WITH_ERRORS`,Part A `/status` 返回 ERROR 清单
- [ ] H4:LLM 5xx/超时自动重试 2-3 次指数退避
- [ ] H5:线程池满载时 `/receive` 返回 503 而非 ok
- [ ] H6:无 token 调 `/trigger` 返回 401
- [ ] H7:并发 plan 用不同参考项目,各自摘要不被串扰
- [ ] M1:修复后 issue 数不降反升时回退上一版本
- [ ] M7:写盘失败时 DB 不更新,无"DB 已更新"误导日志
- [ ] M8:LLM 返回"散文+代码"时截取纯代码;解析失败的文件不落盘
- [ ] L3:FileWriteExecutor 路径遍历测试覆盖绝对路径/`..`/符号链接/白名单外

---

## 附录:与本报告关联的既有文档

- 《后端工作流修改清单.md》— B1–B8 问题清单(生成器产物层面,已落地自检规则)
- `.claude/plans/backend-workflow-fix-plan.md` — B1–B8 修复实施计划(本次已完成)
- 本报告聚焦 B1–B8 之上的**系统性/根本性**缺陷(C1–C3、H1–H9 等),二者互补:清单治"产物字段分裂",本报告治"工作流质量门与一致性"。
